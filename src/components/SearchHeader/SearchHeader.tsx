import React from 'react'
import styles from "./styles.module.scss"

const SearchHeader = () => {
  return (
    <div className={styles.wrapper}>
        <input className={styles.wrapperInput} />
    </div>
  )
}

export default SearchHeader